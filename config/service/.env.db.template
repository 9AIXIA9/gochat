# 模版：专供 mysql 服务使用（请复制为 .env.db 并填写敏感值）
# MySQL 容器内部环境变量
MYSQL_ROOT_PASSWORD=<root_password>
MYSQL_DATABASE=gochat
MYSQL_USER=gochat_app
MYSQL_PASSWORD=<app_db_password>
# 初始化脚本使用的变量
BINLOG_USER=binlog
BINLOG_PASSWORD=<binlog_password>

