-- +goose Up
-- SQL in section 'Up' is executed when this migration is applied
-- This is a baseline migration that does nothing but set versioning
-- You can add initial schema here if desired.

-- No-op baseline

-- +goose Down
-- SQL in section 'Down' is executed when this migration is rolled back
-- For baseline, no-op

